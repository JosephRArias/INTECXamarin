<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LoginXamarin.View.HomePage">
  <!--Pages can be added as references or inline-->
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="ADD" Order="Primary" Clicked="Item_Tapped"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Contacts}"
                  x:Name="listv">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Nombre}"
                              TextColor="Black"
                              Detail="{Binding PhoneNumber}">
                        <TextCell.ContextActions>
                            <MenuItem Clicked="OnMore" Command="{Binding Path=BindingContext.MoreInfoCommand, Source={x:Reference listv}}" Text="More"></MenuItem>
                            <MenuItem Clicked="OnDelete" CommandParameter="{Binding .}" Text="Delete"></MenuItem>
                        </TextCell.ContextActions>
                    </TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>